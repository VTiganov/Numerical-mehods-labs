\documentclass[a4paper,12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T2A]{fontenc}
\usepackage[english, russian]{babel}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{geometry}
\geometry{margin=2.5cm}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{indentfirst}
\usepackage{graphicx}
% Remove fontspec package

\begin{document}
\begin{center}
\small Министерство науки и высшего образования Российской Федерации\\
ФЕДЕРАЛЬНОЕ ГОСУДАРСТВЕННОЕ АВТОНОМНОЕ\\
ОБРАЗОВАТЕЛЬНОЕ УЧРЕЖДЕНИЕ ВЫСШЕГО ОБРАЗОВАНИЯ\\
НАЦИОНАЛЬНЫЙ ИССЛЕДОВАТЕЛЬСКИЙ УНИВЕРСИТЕТ\\
ИТМО
\vfill

\Large\textbf{Лабораторная работа №1}\\
\small{по дисциплине}\\
\small{«Численные методы анализа»}
\vfill
\thispagestyle{empty}

\begin{center}
Выполнили студенты:\\
\vspace{0.3cm}
Бессонов Александр Русланович\\
Группы J3113, ИСУ 465220\\
\vspace{0.3cm}
Тиганов Вадим Игоревич\\
Группы J3112, ИСУ 467701
\end{center}
\vfill

Санкт-Петербург\\
\the\year
\end{center}

\newpage

\section*{Цель работы:}
Изучение и применение методов сингулярного разложения (\verb|SVD|) для анализа
и восстановления изображений. 

\section*{Постановка задачи:}
На основе выбранного изображения требуется провести следующие эксперименты:
\begin{enumerate}
    \item Преобразовать изображение в тензоры цветовых каналов \verb|R, G, B|
    \item Применение степенного метода для выполнения сингулярного разложения каждого цветового канала. Анализ точности восстановления изображения при различном числе итераций \verb|(10, 50, 100)| и сравнение результатов.
    \item Исследование распределения сингулярных чисел для каждого цветового канала и определение минимального числа старших сингулярных чисел, необходимых для приемлемого восстановления изображения.
    \item Нахождение сингулярных чисел одного из цветовых каналов с использованием метода вращений Якоби и сравнение результатов с результатами, полученными степенным методом. Оценка числа операций, выполненных в каждом методе.
\end{enumerate}

\newpage

\section*{Описание данных:}
Было выбрано следующее изображение:

\begin{figure}[h]
    \centering
    \includegraphics[width=0.5\textwidth]{/home/vadim/Edu/Numerical-methods-labs/lab-2/data/pictures/photo.jpeg}
    \caption{Исходное изображение}
    \label{fig:original_image}
\end{figure}
Размер изображения: \verb|640x640| пикселей. Изображение состоит из трех цветовых каналов: \verb|R, G, B|.
\vspace{1cm}

Дополнительно визуализировали каждый цветовой канал:

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{/home/vadim/Edu/Numerical-methods-labs/lab-2/data/pictures/RGB-picture.png}
    \caption{Представление по каналам исходного изображения}
    \label{fig:original_image}
\end{figure}

Размеры изображения и его цветовых каналов при представлении в виде числового массива:
\begin{verbatim}
    Размер изображения: (640, 640, 3)
    Формы каналов: (640, 640), (640, 640), (640, 640)
\end{verbatim}


\section*{Ход работы:}

\subsection*{Преобразование изображения в тензоры цветовых каналов}
Использована библиотека \verb|opencv-python|
\begin{verbatim}
    image_bgr = cv.imread(path_to_image)
    R = image_bgr[:,:,0].astype(float)
    G = image_bgr[:,:,1].astype(float)
    B = image_bgr[:,:,2].astype(float)
\end{verbatim}
Числовое представление изображения представляет собой тензор размерности
\verb|(640,640,3)|, где каждый элемент соответствует тройке \verb|(R,G,B)| для каждого
пикселя слева направо и сверху вниз.\\
Каждый из тензоров цвета представляет собой матрицу размеров \verb|(640,640)|, где каждый элемент представляет собой
яркость каждого из цветовых каналов для каждого пикселя от \verb|0| до \verb|255|. 

\subsection*{Сингулярное разложение с помощью степенного метода}
В этом разделе с помощью степенного метода (\texttt{power\_method\_svd}) приближённо вычисляется наибольшее сингулярное число $\sigma_1$ каждого цветового канала.

\subsubsection*{Алгоритм степенного метода}
Степенной метод позволяет найти наибольшее сингулярное число $\sigma_1$ и соответствующие левый и правый сингулярные векторы $u_1$ и $v_1$ для матрицы $A$.

Алгоритм основан на том, что если матрицу $A$ многократно применять к некоторому начальному вектору $v$, то результат будет стремиться к направлению доминирующего собственного вектора матрицы $A^TA$.

\textbf{Шаги алгоритма:}
\begin{enumerate}
    \item Инициализируем случайный единичный вектор $v \in \mathbb{R}^n$
    \item На каждой итерации вычисляем:
    \begin{align}
        w &= A^T(Av) \\
        v_{new} &= \frac{w}{\|w\|}
    \end{align}
    \item Проверяем условие сходимости: $\|v_{new} - v\| < \varepsilon$, где $\varepsilon$ — заданная точность
    \item После сходимости вычисляем:
    \begin{align}
        u &= Av \\
        \sigma &= \|u\| \\
        u &= \frac{u}{\sigma}
    \end{align}
\end{enumerate}

В результате получаем наибольшее сингулярное число $\sigma$ и соответствующие ему левый и правый сингулярные векторы $u$ и $v$.

% Канал R:
% N=10: 	Ошибка = 32975.45
% N=50: 	Ошибка = 32975.45
% N=100: 	Ошибка = 32975.45
% Канал G:
% N=10: 	Ошибка = 19765.48
% N=50: 	Ошибка = 19765.48
% N=100: 	Ошибка = 19765.48
% Канал B:
% N=10: 	Ошибка = 54508.41
% N=50: 	Ошибка = 54508.41
% N=100: 	Ошибка = 54508.41
\subsubsection*{Результаты применения степенного метода}

% Для оценки точности приближения строится ранг-1 матрица:
% $$
% \tilde{A} = \sigma_1 \cdot u_1 v_1^T
% $$
% и вычисляется евклидова ошибка восстановления:
% $$
% \text{Ошибка} = \left\| A - \tilde{A} \right\|_2
% $$
Для оценки точности приближения строится ранг-1 матрица:
$$
\tilde{A} = \sigma_1 \cdot u_1 v_1^T
$$
и вычисляется ошибка восстановления с помощью нормы Фробениуса:
$$
\text{Ошибка} = \left\| A - \tilde{A} \right\|_F = \sqrt{\sum_{i=1}^{m}\sum_{j=1}^{n} \left(A_{ij} - \tilde{A}_{ij}\right)^2}
$$
где $A$ - исходный цветовой канал изображения, а $\tilde{A}$ - его приближение ранга 1, полученное с помощью степенного метода. Норма Фробениуса представляет собой корень из суммы квадратов всех элементов матрицы разности и является естественной мерой отклонения между матрицами.
Для оценки точности и сходимости алгоритма мы измеряли ошибку восстановления при различном числе итераций. Результаты представлены в таблице \ref{tab:power_method_error}.

\begin{table}[h]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{Цветовой канал} & \textbf{10 итераций} & \textbf{50 итераций} & \textbf{100 итераций} \\
\hline
R & 32975.45 & 32975.45 & 32975.45 \\
\hline
G & 19765.48 & 19765.48 & 19765.48 \\
\hline
B & 54508.41 & 54508.41 & 54508.41 \\
\hline
\end{tabular}
\caption{Ошибка восстановления изображения при различном числе итераций}
\label{tab:power_method_error}
\end{table}

Как видно из таблицы, ошибка восстановления не изменяется при увеличении числа итераций с 10 до 100. Это может говорить о том, что метод быстро сходится на данном изображении, и уже после 10 итераций дальнейшего улучшения не происходит.
\newpage
\section*{Анализ полученных результатов:}


\section*{Выводы и заключение:}

\end{document}
